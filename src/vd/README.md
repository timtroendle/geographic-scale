These scripts are saved [VisiData](https://visidata.org) sessions analysing results. To replay them, you need VisiData and [vdnetcdf](https://github.com/timtroendle/vdnetcdf) installed. Then run them like so:

```bash
vd -p script.vd
```
